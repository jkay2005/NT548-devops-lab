version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo "Tao moi truong ao..."
      - python -m venv venv
      - . venv/bin/activate
      - echo "Cap nhat pip va cai dat cac cong cu ho tro build (Cython, wheel, setuptools)..."
      - pip install --upgrade pip setuptools wheel Cython
      - echo "Dang cai dat cfn-lint va taskcat..."
      - pip install cfn-lint taskcat
  pre_build:
    commands:
      - echo "Kiem tra cu phap CloudFormation bang cfn-lint..."
      - . venv/bin/activate
      - cfn-lint cloudformation/templates/network.yaml
  build:
    commands:
      - echo "Chay Taskcat de test qua trinh tao stack tren AWS..."
      - . venv/bin/activate
      - taskcat test run
  post_build:
    commands:
      - echo "Kiem tra hoan tat!"